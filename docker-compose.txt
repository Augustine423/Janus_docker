services:
  janus:
    image: kyawzayarsoe/janus:latest
    # network_mode: "host"
    environment:
      - PUBLIC_IP=localhost
    volumes:
      - recordings:/opt/janus/share/recordings
    restart: unless-stopped

volumes:
  recordings:

