general: {
        json = "indented"
        ws = true
        ws_port = 8188
        wss = false
}

admin: {
        admin_ws = false
        admin_ws_port = 7188
        admin_wss = false
}

cors: {
}

certificates: {
	cert_pem = "/var/www/http/combined_certificate.pem"
	cert_key = "/var/www/http/aioceaneye.key"
	cert_pwd = "없음"
	#ciphers = "ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256"
}
