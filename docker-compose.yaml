
services:
  janus:
    image: kyawzayarsoe/janus:certv1.0
    environment:
      - PUBLIC_IP=3.39.219.71   # Replace with your actual public IP
    network_mode: "host"
    volumes:
      - recordings:/opt/janus/share/janus/recordings
    restart: unless-stopped

volumes:
  recordings:
